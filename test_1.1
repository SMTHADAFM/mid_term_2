//Schreibe eine Funktion, die ...

//drei Wahrheitswerte übergeben bekommt
//überprüft, ob alle 3 Wahrheitswerte false sind.
//das Ergebnis als Rückgabewert zurückgibt
//(wenn mind. einer true ist, soll false zurückgegeben werden,
//wenn alle false sind, soll true zurückgegeben werden)
boolean a = 50>int(random(100));
boolean b = 50>int(random(100));
boolean c = 50>int(random(100));

void setup() {
  if ( a || b || c != true) {
    println(true);
  } else {
    println(false);
  }
}
//Diesr Test konnte nicht abgegeben werden durch Zeitgründe
