//Gib den Array foo als einen ganzen Satz in einer Zeile in der Konsole aus
//- inklusive Punkt am Ende.
//Der Code sollte ohne weitere Änderungen funktionieren,
//wenn du Worte zu foo hinzufügst oder entfernst. Verwende dafür eine Schleife.


String[] foo = { "Ich", "studiere", "Medieningenieur", "an", "der", "FH", "im", "ersten", "Semester"};

for (int i = 0; i < foo.length; i++) {
  println(join(foo, " ") + ".");
}
